#include "main.h"

/**
 * add - Adds two integers and returns
 * the result.
 *
 * @a: first number.
 * @b: second number.
 *
 * Return: Add of number one and number two.
 */
int add(int a, int b)
{
	return (a + b);
}
