#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - runs the main code
*Return: returns a success value
 */

int main(void)
{
	int a;

	for (a = 48; a <= 57; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
