#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - runs the main code
*Return: returns a success value
 */

int main(void)
{
	int a;

	for (a = 0; a <= 10; a++)
		printf("%d", a);
	printf("\n")
	return (0);
}
